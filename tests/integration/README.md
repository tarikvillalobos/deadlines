# Integration tests

Testes que dependem de infraestrutura real e atravessam os limites internos do backend.

Eles são incluídos no source set de testes do Gradle e usam Testcontainers para evitar dependência de um banco previamente configurado.
