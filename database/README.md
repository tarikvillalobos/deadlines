# Database

Recursos de banco de dados compartilhados pelo projeto.

- `migrations/`: alterações versionadas de schema.
- `seed/`: dados explícitos para desenvolvimento ou inicialização.

O schema será criado exclusivamente por migrations. A aplicação não deverá criar tabelas automaticamente em runtime.
